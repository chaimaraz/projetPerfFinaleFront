import { ParticipantInformation } from './participant-information';

describe('ParticipantInformation', () => {
  it('should create an instance', () => {
    expect(new ParticipantInformation()).toBeTruthy();
  });
});
